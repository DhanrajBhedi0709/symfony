{{ form_start(form) }}
    {{ form_widget(form) }}
    <br/>
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
    <a class="btn btn-primary" href="{{ path('dashboard') }}">back to list</a>
{{ form_end(form) }}
